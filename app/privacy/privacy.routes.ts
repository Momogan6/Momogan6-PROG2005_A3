import { Routes } from '@angular/router';
import { PrivacyPage } from './privacy.page';

// Routing for Privacy page
export const privacyRoutes: Routes = [
  { path: '', component: PrivacyPage }
];